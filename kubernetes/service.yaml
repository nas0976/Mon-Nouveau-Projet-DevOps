apiVersion: v1
kind: Service
metadata:
  name: wordpress-service
  namespace: jenkins
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
  selector:
    app: wordpress # Assure-toi que ce sélecteur correspond au 'labels' de ton fichier wordpress-deployment.yaml
